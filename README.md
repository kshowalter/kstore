# K Store
Simple DB like javascript object storage

## API

### insert(input)

### find(request)
    return documentArray;
    } else {
    return false;

### findOne(request)
    return data[_id];
    } else {
    return false;

### options(name)
    return Object.keys(this.index[name]);
